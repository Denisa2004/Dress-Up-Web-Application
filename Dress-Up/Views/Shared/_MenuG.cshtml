@{
    // Meniul pentru guest, acum cu butoane fixate in dreapta jos si umbre roz
}

<div class="menu">
   
    <a href="@Url.Action("Index", "Home")"
       class="menu-button">
        Create Account
    </a>

    
    <a href="@Url.Action("Index", "Avatar")"
       class="menu-button">
        Create One Outfit
    </a>

   
    <a href="@Url.Action("Index", "Home")"
       class="menu-button">
        Log Out
    </a>
</div>

<style>
    .menu {
        position: fixed;
        bottom: 20px; /* Plasează meniul la 20px de jos */
        right: 20px; /* Plasează meniul la 20px de dreapta */
        display: flex;
        flex-direction: column; /* Aliniază butoanele vertical */
        gap: 15px; /* Spațiu între butoane */
        z-index: 1000;
    }

    .menu-button {
        padding: 12px 25px;
        background-color: #f7b1c1;
        color: white;
        text-decoration: none;
        border-radius: 25px; 
        font-size: 16px;
        font-family: 'Arial', sans-serif;
        text-align: center;
        box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2); 
        transition: all 0.3s ease; 
        width: 180px; 
        text-align: center;
    }

        .menu-button:hover {
            background-color: #f14f86; 
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3); 
            transform: scale(1.1); 
        }

        .menu-button:active {
            background-color: #e02d63; 
            transform: scale(1); 
        }

    body {
        margin-bottom: 80px;
    }
</style>
